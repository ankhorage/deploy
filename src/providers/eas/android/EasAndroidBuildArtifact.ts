export interface EasAndroidBuildArtifact {
  readonly buildId: string;
  readonly buildProfile: string;
  readonly fingerprint: string;
  readonly versionCode: number;
  readonly archiveUrl: string;
}
